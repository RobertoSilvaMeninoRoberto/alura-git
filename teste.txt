  
  
   git config --local 
   git config --global.

 git config --local user.name "seu nome" , após o qual pressionaremos "Enter".

git config user.name,  
git config user.email. 
git log

ocupa uma única linha: git log --oneline. 
 como as alterações do commit, usaremos git log -p. 
 